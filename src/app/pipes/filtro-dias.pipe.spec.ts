import { FiltroDiasPipe } from './filtro-dias.pipe';

describe('FiltroDiasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDiasPipe();
    expect(pipe).toBeTruthy();
  });
});
